{
  "devDependencies": {
    "fuse-box": "^4.0.0",
    "terser": "^5.5.1",
    "typescript": "^4.1.3",
    "uglify-js": "^3.12.3",
    "@aurelia/plugin-conventions": "dev",
    // @if less
    "less": "^4.0.0",
    // @endif
    // @if sass
    "node-sass": "^5.0.0",
    // @endif
    // @if css-module
    "postcss-modules": "^4.0.0",
    // @endif
  },
  "scripts": {
    "start": "node fuse",
    "build": "node fuse build"
  }
}
