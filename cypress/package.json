{
  "devDependencies": {
    "cypress": "^3.0.0",
    "babel-eslint": "^10.0.0",
    "eslint": "^6.0.0",
    // @if shadow-dom-open || shadow-dom-closed
    "cypress-shadow-dom": "^1.0.0",
    // @endif
  },
  "scripts": {
    "test:e2e": "cypress run"
  }
}
