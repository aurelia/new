{
  "devDependencies": {
    "cypress": "^4.0.0",
    "eslint-plugin-cypress": "^2.0.0",
    // @if shadow-dom
    "cypress-shadow-dom": "^1.0.0",
    // @endif
  },
  "scripts": {
    "test:e2e": "cypress run"
  }
}
