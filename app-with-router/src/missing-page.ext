export class MissingPage {
  /* @if typescript */public /* @endif */static parameters = ['id'];
  /* @if typescript */public /* @endif */missingComponent/* @if typescript */: string /* @endif */;

  /* @if typescript */public /* @endif */load(parameters/* @if typescript */: {id: string}/* @endif */)/* @if typescript */: void/* @endif */ {
    this.missingComponent = parameters.id;
  }
}
