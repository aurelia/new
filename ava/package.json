{
  "devDependencies": {
    "ava": "^3.8.2",
    "jsdom": "^16.2.2",
    "jsdom-global": "^3.0.2",
    // @if babel
    "@ava/babel": "^1.0.1",
    "@babel/register": "^7.9.0",
    // @endif
    // @if typescript
    "@ava/typescript": "^1.1.1",
    "ts-node": "^8.10.1",
    // @endif
  },
  "scripts": {
    "test": "ava"
  }
}
