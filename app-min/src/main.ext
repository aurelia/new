// @if browser-ie11
// Promise polyfill for old browsers
import 'promise-polyfill/lib/polyfill';
// @endif
import Aurelia/* @if shadow-dom-open || shadow-dom-closed || css-module */, { StyleConfiguration }/* @endif */ from 'aurelia';
import { MyApp } from './my-app';

Aurelia/* @if shadow-dom-open || shadow-dom-closed */.register(StyleConfiguration.shadowDOM())/* @endif *//* @if css-module */.register(StyleConfiguration.cssModulesProcessor())/* @endif */.app(MyApp).start();
