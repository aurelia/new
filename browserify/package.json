{
  "devDependencies": {
    "gulp": "^4.0.0",
    "vinyl-source-stream": "^2.0.0",
    "browser-sync": "^2.0.0",
    "connect-history-api-fallback": "^1.0.0",
    "browserify": "^16.0.0",
    "watchify": "^3.0.0",
    // @if typescript
    "tsify": "^4.0.0",
    // @endif
    // @if babel
    "babelify": "^10.0.0",
    // @endif
    "stringify": "^5.0.0",
  },
  "scripts": {
    "start": "gulp",
    "build": "gulp build"
  }
}