{
  "devDependencies": {
    "rimraf": "^3.0.2",
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "start": "parcel index.html -p 9000 --open",
    "build": "rimraf dist && parcel build index.html"
  }
}
